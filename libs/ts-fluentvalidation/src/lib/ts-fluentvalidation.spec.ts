import { tsFluentvalidation } from './ts-fluentvalidation';

describe('tsFluentvalidation', () => {
  it('should work', () => {
    expect(tsFluentvalidation()).toEqual('ts-fluentvalidation');
  });
});
